module.exports = function (msg, args) {
    msg.reply("has passed away 4 time(s)");
    //msg.channel.send("hello amigo"); //this doesnt tag the person its replying to
    
    //diff replies
    //const r = Math.floor(Math.random() * deathReplies.length);
    //msg.channel.send(replies[r]);
};
